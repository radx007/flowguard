services:
  postgres:
    image: postgres:17
    container_name: flowguard-postgres
    environment:
      POSTGRES_DB: flowguard
      POSTGRES_USER: flowguard
      POSTGRES_PASSWORD: flowguard
    ports:
      - "5432:5432"
    volumes:
      - flowguard-postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U flowguard -d flowguard"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  flowguard-postgres-data: